import React, {useState} from 'react';
import IVariableDataObject from '../interfaces/IVariableDataObject';

const GearingCalculator = () => {
	const [gearingSetups, setGearingSetups] = useState([]);

	
};

export default GearingCalculator;